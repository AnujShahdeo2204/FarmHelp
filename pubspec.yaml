name: farm_help
description: "A new Flutter project."
publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # Use compatible version of HTTP for Gemini
  http: ^1.1.0

  # Google Gemini AI
  google_generative_ai: ^0.2.1

  # Chat UI
  dash_chat_2: ^0.0.18

  # Environment variable support
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Uncomment and add your images here if you need assets
  # assets:
  #   - assets/images/
